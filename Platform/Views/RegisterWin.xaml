<Window x:Class="Platform.Views.RegisterWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Platform.Views"
        mc:Ignorable="d"
        Title="RegisterWin" Height="520" Width="300"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:helper="clr-namespace:Platform.Helper;assembly=Platform.Helper"
        x:Name="RegisterWindow">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Platform.CommonResource;component/Styles/ButtonStyles.xaml"/>

                <ResourceDictionary>
                    <Style TargetType="TextBox" x:Key="UserNameTextBoxStyle">
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Border BorderBrush="#DDD"
                                            BorderThickness="0,0,0,1" Height="40" 
                                            SnapsToDevicePixels="True" Name="border">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="30"/>
                                                <ColumnDefinition/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="&#xe610;"
                                                       FontFamily="{StaticResource Iconfont}" 
                                                       VerticalAlignment="Center" 
                                                       HorizontalAlignment="Center" 
                                                       FontSize="15">
                                                <TextBlock.Foreground>
                                                    <LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
                                                        <GradientStop Color="#16a1ff" Offset="0"/>
                                                        <GradientStop Color="#b4fee7" Offset="0.65"/>
                                                        <GradientStop Color="#16a1ff" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </TextBlock.Foreground>
                                            </TextBlock>
                                            <TextBlock Text="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" Grid.Column="1" VerticalAlignment="Center"
                                                       Foreground="#DDD" Name="markText" Visibility="Collapsed"/>
                                            <ScrollViewer Name="PART_ContentHost" Grid.Column="1"/>
                                        </Grid>
                                    </Border>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="true">
                                            <Setter Property="BorderBrush" TargetName="border" Value="#FF7EB4EA"/>
                                        </Trigger>
                                        <Trigger Property="IsKeyboardFocused" Value="true">
                                            <Setter Property="BorderBrush" TargetName="border" Value="#FF569DE5"/>
                                        </Trigger>
                                        <DataTrigger Binding="{Binding Path=Text,RelativeSource={RelativeSource Mode=Self}}" Value="">
                                            <Setter Property="Visibility" TargetName="markText" Value="Visible"/>
                                        </DataTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <Style TargetType="PasswordBox" x:Key="PasswordBoxStyle">
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="PasswordBox">
                                    <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Height="40" SnapsToDevicePixels="True" Name="border">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="30"/>
                                                <ColumnDefinition/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="&#xe602;" FontFamily="{StaticResource Iconfont}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="15">
                                                <TextBlock.Foreground>
                                                    <LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
                                                        <GradientStop Color="#16a1ff" Offset="0"/>
                                                        <GradientStop Color="#b4fee7" Offset="0.65"/>
                                                        <GradientStop Color="#16a1ff" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </TextBlock.Foreground>
                                            </TextBlock>
                                            <TextBlock Text="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" Grid.Column="1" VerticalAlignment="Center" Foreground="#DDD" Name="markText"
                                             Visibility="Collapsed"/>
                                            <ScrollViewer Name="PART_ContentHost" Grid.Column="1"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="true">
                                            <Setter Property="BorderBrush" TargetName="border" Value="#FF7EB4EA"/>
                                        </Trigger>
                                        <Trigger Property="IsKeyboardFocused" Value="true">
                                            <Setter Property="BorderBrush" TargetName="border" Value="#FF569DE5"/>
                                        </Trigger>
                                        <DataTrigger Binding="{Binding Path=(helper:PwdHelper.CustPwd), RelativeSource={RelativeSource Mode=Self}}" Value="">
                                            <Setter Property="Visibility" TargetName="markText" Value="Visible"/>
                                        </DataTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

    </Window.Resources>

    <Grid ClipToBounds="True">
        <Ellipse Width="400" Height="400" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,-200,-200,0">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="AliceBlue" Offset="0"/>
                    <GradientStop Color="Azure" Offset="1"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
        </Ellipse>
        <Ellipse Width="400" Height="400" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="-180,-180,0,0">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="CadetBlue" Offset="0"/>
                    <GradientStop Color="AliceBlue" Offset="1"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
        </Ellipse>

        <Grid Margin="35,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="20"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>

            <TextBox Grid.Row="1" Tag="请输入真实姓名" VerticalContentAlignment="Center" Style="{StaticResource UserNameTextBoxStyle}" Text="{Binding SysUser.Account}" Margin="0 10"/>

            <TextBox Grid.Row="2" Tag="请输入账号" VerticalContentAlignment="Center" Style="{StaticResource UserNameTextBoxStyle}" Text="{Binding SysUser.RealName}" Margin="0 10"/>

            <PasswordBox Grid.Row="3" Tag="请输入密码" VerticalContentAlignment="Center" Style="{StaticResource PasswordBoxStyle}" helper:PwdHelper.IsOpen="True"  helper:PwdHelper.CustPwd="{Binding tempPwd, Mode=TwoWay}"/>

            <PasswordBox Grid.Row="4" Tag="请再次输入密码" VerticalContentAlignment="Center" Style="{StaticResource PasswordBoxStyle}" helper:PwdHelper.IsOpen="True"  helper:PwdHelper.CustPwd="{Binding SysUser.Password, Mode=TwoWay}"/>

            <TextBox Grid.Row="5" Tag="请输入部门" VerticalContentAlignment="Center" Style="{StaticResource UserNameTextBoxStyle}" Text="{Binding SysUser.Department}" Margin="0 10"/>

            <TextBox Grid.Row="6" Tag="请输入性别" VerticalContentAlignment="Center" Style="{StaticResource UserNameTextBoxStyle}" Text="{Binding SysUser.Gender}" Margin="0 10"/>

            <TextBox Grid.Row="7" Tag="请输入手机号" VerticalContentAlignment="Center" Style="{StaticResource UserNameTextBoxStyle}" Text="{Binding SysUser.Phone}" Margin="0 10"/>

            <Button Content="注册" Grid.Row="8" Height="35" Style="{StaticResource NormalButtonStyle}" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=RegisterWindow}"/>

            <TextBlock Text="{Binding LoginErrorMsg}" Grid.Row="9" Foreground="Red" VerticalAlignment="Center" HorizontalAlignment="Center" TextWrapping="Wrap"/>
        </Grid>

        <Button Content="&#xe653;" FontFamily="{StaticResource Iconfont}" VerticalAlignment="Bottom" HorizontalAlignment="Right" Style="{StaticResource WindowControlButtonStyle}" Width="40" Height="30" FontSize="14" Margin="0,0,5,475"  WindowChrome.IsHitTestVisibleInChrome="True" Click="Close_Click"/>
    </Grid>
</Window>
